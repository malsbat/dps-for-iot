sudo: required

matrix:
  include:
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - doxygen
            - g++-7
            - zlib1g-dev
      env:
         - MATRIX_EVAL="CC=gcc-7 && CXX=g++-7"

compiler:
  - gcc

env:
  - VARIANT=debug TRANSPORT=udp
  - VARIANT=debug TRANSPORT=tcp
  - VARIANT=debug TRANSPORT=dtls
  - VARIANT=release TRANSPORT=udp
  - VARIANT=release TRANSPORT=tcp
  - VARIANT=release TRANSPORT=dtls

before_install:
  - eval "${MATRIX_EVAL}"
  - ./.travis/install-swig.sh
  - ./.travis/install-nodejs.sh

language: c
script:
  - eval "${MATRIX_EVAL}"
  - scons variant=$VARIANT transport=$TRANSPORT CC=$CC CXX=$CXX UV_PATH=ext/libuv
